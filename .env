# ==============================================
# 🏠 本地开发环境配置 (Brain)
# 用于在本地运行智能体逻辑，并远程调用云端接口
# ==============================================

# [核心] 远程网关地址
# 指向您部署了 Gateway 的云服务器 IP 地址
# 如果是在本地测试 Gateway，则保持 http://localhost:8000
# ❌ 不要填 Google 的 API 地址
# ✅ 填您的云服务器地址: http://1.2.3.4:8000
GATEWAY_API_BASE=http://localhost:8000

# [安全] 网关访问密钥
# 必须与云端 .env 中的 GATEWAY_SECRET 完全一致！
GATEWAY_SECRET=sk-my-super-secret-password-change-me

# [记忆] Pinecone 向量数据库配置
# 用于智能体的长期记忆存储
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_ENVIRONMENT=gcp-starter
VECTOR_INDEX_NAME=swarm-memory

# [兼容性] Gemini API Keys 占位符
# 虽然 Brain 现在通过 Gateway 调用模型，
# 但为了防止 brain/config/keys.py 启动报错，
# 这里建议保留一个空列表或假值，或者修改代码移除该检查。
GEMINI_API_KEYS=["placeholder-key-managed-by-gateway"]
